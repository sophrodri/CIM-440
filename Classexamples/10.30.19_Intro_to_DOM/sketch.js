function setup() {
  // put setup code here
  createCanvas(400,400);
  // msot dom functions go on the setup? to stop looping continuously
  cnv.id("mycanvas";)

  var container0 = createDiv();// designates a section on the page 
}

function draw() {
  // put drawing code here
  background(0);
}
